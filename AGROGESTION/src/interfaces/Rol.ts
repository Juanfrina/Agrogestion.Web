/**
 * @file Rol.ts
 * @description Re-exportación del enum Rol desde lib/types.
 *
 * Igual que con Perfil, esto es para que puedas importar Rol
 * desde interfaces/ si te resulta más cómodo.
 */

export { Rol } from '../lib/types';
